openapi: "3.0.3"
info:
  title: example API
  version: "1.0.0"
paths:
  /users:
    delete:
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRequest'
components:
  schemas:
    UserRequest:
      type: object
    CustomerResponse:
      type: object
    OrderDelete:
      type: object
    ChargeUpdate:
      type: object