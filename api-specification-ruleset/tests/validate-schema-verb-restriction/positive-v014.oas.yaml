openapi: "3.0.3"
info:
  title: example API
  version: "1.0.0"
paths:
  /users:
    delete:
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserDTO'
components:
  schemas:
    UserDTO:
      type: object
    Customer:
      type: object
    Order:
      type: object
    Charge:
      type: object