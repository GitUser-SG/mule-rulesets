openapi: "3.0.0"
info:
  title: example API
  version: "1.0.0"
paths:
  /users:
    get:
      parameters:
        - name: users
          schema:
            type: string
        - name: order_status
          schema:
            type: string
        - name: customer_type_id
          schema:
            type: string